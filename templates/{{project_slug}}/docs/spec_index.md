# Specification Index

This index summarizes the active specifications for {{ project_name }}. For the authoritative tables, see `docs/specs/spec_index.md`, which includes links to PRDs, SDS, ADRs, and technical specs.

- Update this file alongside `docs/specs/spec_index.md` when the spec set changes.
- Regenerate the traceability matrix with `pnpm spec:matrix` to keep references fresh.

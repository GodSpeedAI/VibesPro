-- Seed data for {{ project_name }}
-- This file is run by `just db-seed` to populate the database with initial data
-- Add your seed data below

-- Example: Insert a default user
-- INSERT INTO public.users (id, email, is_active)
-- VALUES ('00000000-0000-0000-0000-000000000001', 'admin@example.com', true)
-- ON CONFLICT (id) DO NOTHING;
